# Copy this file to .env and fill in real values. Do NOT commit real secrets.

# MongoDB connection string (replace <username>, <password>, <dbname> as needed)
MONGO_URI=mongodb+srv://microBlog_DB:microBlog_DB@cluster0.rauitns.mongodb.net/microblog?retryWrites=true&w=majority

# JWT secret (use a long random string in production)
JWT_SECRET=change_this_to_a_long_random_value

# Frontend origin (used for CORS)
FRONTEND_URL=http://localhost:5173

# Server port (optional, default 5000)
PORT=5000

# Node environment
NODE_ENV=development
